# pnpm 11 re-verifica el estado de dependencias antes de correr cada script
# (dev/build/test). Lo desactivamos: la verificación la hacemos explícita con
# "pnpm install", y así editar el pnpm-workspace.yaml no rompe los scripts.
verify-deps-before-run=false
